eureka:
  client:
    registry-fetch-interval-seconds: 5
    service-url:
      defaultZone: http://discovery/eureka/
server:
  port: 80
spring:
  application:
    name: consumer
provider:
  random-greeting:
    resource-uri: /greetings
    service-id: provider
